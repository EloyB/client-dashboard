import { EmptyState } from '@/components/shared/EmptyState';

/** Filled in by slice 6a. */
export function DocumentsTabPlaceholder() {
  return (
    <EmptyState
      variant="neutral"
      title="Nog geen documenten"
      description="Documenten voor dit project komen hier terecht."
    />
  );
}
